
const GoogleCalendar = ({setNumber}) => {
    return(
        <div>
            <p>TODO</p>
            <button onClick={() => setNumber(0)}>Wróć</button>
        </div>
    );
}

export default GoogleCalendar;